export const API_TAG_CATEGORIES = 'Caregories';
export const API_TAG_CARS = 'Cars';
export const API_TITLE = 'Api locadora de carros';
export const API_DESCRIPTION =
  'This API aims to test my knowledge in Back-End. Developed using: NodeJS with TypeScript, ExpressJS, PostgreSQL database, TypeORM.';
export const API_VERSION = '1.0';

export const summaryCreate = (entity) =>
  `Responsible for the ${entity} registration`;

export const summaryGetAll = (entity) =>
  `Responsible for obtaining the ${entity} registrations`;

export const summaryGetOne = (entity) =>
  `Responsible for obtaining the ${entity} registration`;

export const summaryUpdate = (entity) =>
  `Responsible for updating the ${entity} registration`;

export const summaryDelete = (entity) =>
  `Responsible for deleting the ${entity} registration`;
